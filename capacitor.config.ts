import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'hivex.com',
  appName: 'Hivexlat',
  webDir: 'dist/hivexlat',
  server: {
    androidScheme: 'https'
  }
};

export default config;
